/* Eliminate the comment by fading it out */
$('#micropost_<%= @micropost.id %>').html("");
/* Replace the count of comments */
$("#microposts_count").html("<%= pluralize(current_user.microposts.count, 'Comentari') %>");
